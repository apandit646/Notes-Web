<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.0/css/all.min.css"
        integrity="sha512-PgQMlq+nqFLV4ylk1gwUOgm6CtIIXkKwaIHp/PAIWHzig/lKZSEGKEysh0TCVbHJXCLN7WetD8TFecIky75ZfQ=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />

    <title>Submit Complaint</title>

    <style>
        :root {
            --primary-color: #00fff2;
            --secondary-color: #0b1c2c;
            --accent-color: #ff00d4;
            --text-color: #ffffff;
        }

        body {
            background: linear-gradient(45deg, #000428, #004e92);
            color: var(--text-color);
            font-family: 'Segoe UI', sans-serif;
            min-height: 100vh;
            position: relative;
            overflow-x: hidden;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 30%, rgba(0, 255, 242, 0.1) 0%, transparent 20%),
                radial-gradient(circle at 80% 70%, rgba(255, 0, 212, 0.1) 0%, transparent 20%);
            pointer-events: none;
        }

        .navbar {
            background: rgba(11, 28, 44, 0.9);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid var(--primary-color);
        }

        .navbar-brand {
            font-size: 1.5rem;
            color: var(--primary-color);
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .navbar-brand i {
            color: var(--accent-color);
            animation: pulse 2s infinite;
        }

        .nav-link {
            color: var(--text-color) !important;
            position: relative;
            padding: 5px 15px !important;
            transition: all 0.3s ease;
        }

        .nav-link::before {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            width: 0;
            height: 2px;
            background: var(--primary-color);
            transition: all 0.3s ease;
        }

        .nav-link:hover::before {
            width: 100%;
            left: 0;
        }

        .main_header {
            background: linear-gradient(135deg, rgba(0, 255, 242, 0.1), rgba(255, 0, 212, 0.1));
            backdrop-filter: blur(10px);
            padding: 60px 0;
            text-align: center;
            border-bottom: 1px solid rgba(0, 255, 242, 0.2);
        }

        .main_header h1 {
            color: var(--primary-color);
            font-weight: 300;
            letter-spacing: 3px;
            margin-bottom: 20px;
        }

        .main_header span {
            color: var(--accent-color);
        }

        .main_header p {
            color: var(--text-color);
            font-size: 1.2rem;
            opacity: 0.8;
        }

        .complaint-form {
            background: rgba(11, 28, 44, 0.8);
            backdrop-filter: blur(20px);
            border-radius: 20px;
            border: 1px solid rgba(0, 255, 242, 0.1);
            padding: 40px;
            margin-top: 50px;
            box-shadow: 0 0 30px rgba(0, 255, 242, 0.2);
            animation: containerGlow 3s infinite alternate;
        }

        .complaint-form h3 {
            color: var(--primary-color);
            text-align: center;
            margin-bottom: 30px;
            font-weight: 300;
            letter-spacing: 3px;
        }

        .form-control {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(0, 255, 242, 0.2);
            border-radius: 10px;
            color: var(--text-color);
            padding: 12px;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            background: rgba(255, 255, 255, 0.1);
            border-color: var(--primary-color);
            box-shadow: 0 0 15px rgba(0, 255, 242, 0.3);
            color: var(--text-color);
        }

        .btn-custom {
            background: linear-gradient(45deg, var(--primary-color), var(--accent-color));
            border: none;
            border-radius: 10px;
            color: var(--secondary-color);
            font-weight: bold;
            padding: 12px 30px;
            text-transform: uppercase;
            letter-spacing: 2px;
            transition: all 0.3s ease;
            margin-top: 20px;
        }

        .btn-custom:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(0, 255, 242, 0.4);
        }

        .form-group label {
            color: var(--primary-color);
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 8px;
        }

        .text_res {
            color: var(--accent-color);
            margin-top: 15px;
            text-align: center;
            font-size: 0.9rem;
            letter-spacing: 1px;
        }

        .floating-footer {
            background: rgba(11, 28, 44, 0.9);
            backdrop-filter: blur(10px);
            padding: 20px;
            color: var(--text-color);
            border-top: 1px solid var(--primary-color);
        }

        .floating-footer span {
            color: var(--accent-color);
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.2); }
            100% { transform: scale(1); }
        }

        @keyframes containerGlow {
            from { box-shadow: 0 0 20px rgba(0, 255, 242, 0.2); }
            to { box-shadow: 0 0 40px rgba(0, 255, 242, 0.4); }
        }

        @media (max-width: 768px) {
            .complaint-form {
                margin: 20px;
                padding: 20px;
            }
        }
    </style>
</head>

<body>
    {{>navbar}}

    <div class="main_header">
        <h1>Welcome to <span>Anubhav Pvt. Ltd</span></h1>
        <p>Your trusted partner for the best weather updates!</p>
    </div>

    <div class="container">
        <div class="complaint-form">
            <h3>Submit Your Complaint</h3>
            <form id="complaint_form">
                <div class="form-group">
                    <label for="email">Email Address</label>
                    <div class="input-group">
                        <div class="input-group-prepend">
                            <span class="input-group-text"><i class="fas fa-envelope"></i></span>
                        </div>
                        <input type="email" class="form-control" id="email" placeholder="Enter your email" required>
                    </div>
                </div>
                <div class="form-group">
                    <label for="complaint">Your Complaint</label>
                    <div class="input-group">
                        <div class="input-group-prepend">
                            <span class="input-group-text"><i class="fas fa-comment-alt"></i></span>
                        </div>
                        <textarea class="form-control" id="complaint" rows="5" 
                            placeholder="Please describe your complaint in detail..." required></textarea>
                    </div>
                </div>
                <h6 class="text_res" id="textCon">Enter Complaint</h6>
                <button type="submit" class="btn btn-custom btn-block" id="submitBtn">
                    <i class="fas fa-paper-plane mr-2"></i>Submit Complaint
                </button>
            </form>
        </div>
    </div>

    <footer class="floating-footer text-center mt-5">
        <p>Created with <span>&hearts;</span> by Anubhav</p>
    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.min.js"></script>
    <script src="js/complain.js"></script>
</body>
</html>